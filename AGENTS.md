# Agent notes

Normative behavior is in `SPEC.md`. README and `example-config.yaml` are the operator-facing restatement.

When changing listen, forward, handlers, or tsnet lifecycle, update `SPEC.md` invariants in the same change.
